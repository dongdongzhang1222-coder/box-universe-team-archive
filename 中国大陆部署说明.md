# BOX UNIVERSE V8 中国网络版

## 本地运行

双击 `启动中国网络版.command`，然后访问 `http://localhost:8767`。

不要直接双击 `index.html`：直接打开只能显示静态内容，无法共享保存上传、点赞和弹幕。

## 已完成的中国网络兼容改造

- 页面资源全部使用同域相对地址，不再要求从 GitHub Pages 载入。
- 点赞和弹幕改为 `/api` 同域接口，不再依赖 Cloudflare Workers。
- 案例、IDEA BANK 文件、快乐支线照片/视频均可保存到服务器目录。
- “小料自取”固定在右上方，内含两份 PDF 的预览与下载。
- 722 MB 品牌规范另备 9.6 MB 网页预览版，高清原版仍可下载。
- 境外网络字体已移除，使用大陆设备自带中文字体回退。

## 正式上线建议

将整个目录部署到中国大陆的云服务器，并使用 Node.js 20 运行 `server.js`。建议配合 Nginx、HTTPS、进程守护和已备案域名。服务器需要为 `data/` 与 `uploads/` 提供持久磁盘和备份。

当前版本面向团队内部使用。若公开到互联网，应在上传和删除接口前增加登录或管理员口令，并接入内容安全审核。
